# Red-Teaming

Red-team tools are useful for known attack patterns, but they do not automatically know an application's unique rules. One agent must never expose another customer's records; another must never issue a refund without verification. AI Agent Guardian turns those app-specific constraints into attack objectives.
